<!-- implemented in ConfigForm->generateForm() -->
<!-- https://codeberg.org/QuinQuies/glpisaml/issues/13 -->
{% import glpi_tpl_macro as fields %}

<script>
    // Remember tab state
    $(function() {
        $('a[data-bs-toggle="tab"]').on('shown.bs.tab', function (e) {
            localStorage.setItem('lastTab', $(this).attr('href'));
        });
        var lastTab = localStorage.getItem('lastTab');
        
        if (lastTab) {
            $('[href="' + lastTab + '"]').tab('show');
        }
    });
    // Clipboard function
    function copyClip(key) {
        // Get the text field
        var copyText = document.getElementById(key);
        // Select the text field
        copyText.select();
        copyText.setSelectionRange(0, 99999); // For mobile devices
        // Copy the text inside the text field
        navigator.clipboard.writeText(copyText.value);
        // Alert the copied text
        alert("Succesfully copied the value: " + copyText.value + " to your clipboard.");
    }
</script>

<form name="form" method="post" action="{{ glpi_rootdoc }}" id="phpsaml_config">
<div class="phpsaml_config_wrapper" id="samlForm">
    <input type="hidden" name="{{ inputfields.id.field }}" value="{{ inputfields.id.value }}">

    <div class="card" style="min-height:100%;">
        <!-- Input fields -->
        <ul class="nav nav-tabs" id="samlTabs" role="tablist">
            <li class="nav-item" role="presentation">
                <a class="nav-link active" 
                   id="general-tab" 
                   data-bs-toggle="tab" 
                   href="#general" 
                   data-bs-target="#general" 
                   type="button" role="tab" 
                   aria-controls="general" 
                   aria-selected="true"><b>{{ __("General") }} {{ general_warning }}</b></a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" 
                   id="transit-tab" 
                   data-bs-toggle="tab" 
                   href="#transit" 
                   data-bs-target="#transit" 
                   type="button" 
                   role="tab" 
                   aria-controls="transit" 
                   aria-selected="false"><b>{{ __('Transit', plugin) }} {{ transit_warning }}</b></a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" 
                   id="provider-tab" 
                   data-bs-toggle="tab" 
                   href="#provider" 
                   data-bs-target="#provider" 
                   type="button" 
                   role="tab" 
                   aria-controls="provider" 
                   aria-selected="false"><b>{{ __('Service provider', plugin) }} {{ provider_warning }}</b></a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" 
                   id="idp-tab" 
                   data-bs-toggle="tab" 
                   href="#idp" 
                   data-bs-target="#idp" 
                   type="button" 
                   role="tab" 
                   aria-controls="idp" 
                   aria-selected="false"><b>{{ __('Identity provider', plugin) }} {{ idp_warning }}</b></a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link" 
                   id="security-tab" 
                   data-bs-toggle="tab" 
                   href="#security" 
                   data-bs-target="#security" 
                   type="button" 
                   role="tab" 
                   aria-controls="security" 
                   aria-selected="false"><b>{{ __('Security') }} {{ security_warning }}</b></a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link"
                   id="scim-tab" 
                   data-bs-toggle="tab" 
                   href="#scim" 
                   data-bs-target="#scim" 
                   type="button" 
                   role="tab" 
                   aria-controls="scim" 
                   aria-selected="false"><b>{{ __('SCIM', plugin) }} [NEW] {{ scim_warning }}</b></a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link"
                   id="logging-tab" 
                   data-bs-toggle="tab" 
                   href="#logging" 
                   data-bs-target="#logging" 
                   type="button" 
                   role="tab" 
                   aria-controls="logging" 
                   aria-selected="false"><b>{{ __('Logging') }}</b></a>
            </li>
        </ul>
        <div class="tab-content flex-fill glpisaml" id="myTabContent">
           <!-- ################################################################################-->
            <!-- General details -->
            <div class="tab-pane fade show active" id="general" role="tabpanel" aria-labelledby="general-tab">
                <div class="card-body">
                    <div class="row g-2">
                        <!-- name field -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.name.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.name.errors }}</div>
                                {% endif %}
                                {% if buttonsHiddenWarn == true %}
                                    <div class="form-text text-helper" style="color:rgb(180, 120, 8)">⚠️ Userdomain is configured. The login button will be hidden from the loginscreen. Clear the Userdomain to make the button visable again.</div>
                                {% endif %}
                            {% endset %}

                            {{ fields.textField(
                                inputfields.name.field, 
                                inputfields.name.value, 
                                inputfields.name.formtitle, 
                                {
                                    'add_field_html': error_helper,
                                    helper: inputfields.name.formexplain,
                                }
                            ) }}
                        </div>
                   
                        <!-- icon field -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.conf_icon.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.conf_icon.errors }}</div>
                                {% endif %}
                                {% if buttonsHiddenWarn == true %}
                                    <div class="form-text text-helper" style="color:rgb(180, 120, 8)">⚠️ Userdomain is configured. The login button will be hidden from the loginscreen. Clear the Userdomain to make the button visable again.</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.textField(
                                inputfields.conf_icon.field, 
                                inputfields.conf_icon.value, 
                                inputfields.conf_icon.formtitle, 
                                {
                                    'add_field_html': error_helper,
                                    helper: inputfields.conf_icon.formexplain,
                                }
                            ) }}
                        </div>

                        <!-- domain field -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.conf_domain.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.conf_domain.errors }}</div>
                                {% endif %}
                                {% if buttonsHiddenWarn == true %}
                                    <div class="form-text text-helper" style="color:rgb(3, 126, 26)">🔒 Userdomain is configured. Users can trigger a Saml Login by providing their {username}@{userdomain} in the glpi username field and pressing the login button. The password field is ignored and can be left blank. This is a more secure option that wont expose configured IDPs.
                                    </div>
                                {% endif %}
                            {% endset %}

                            {{ fields.textField(
                                inputfields.conf_domain.field, 
                                inputfields.conf_domain.value, 
                                inputfields.conf_domain.formtitle, 
                                {
                                    'add_field_html': error_helper,
                                    helper: inputfields.conf_domain.formexplain,
                                }
                            ) }}
                        </div>

                        <!-- Comment field -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.comment.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.comment.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.textareaField(
                                inputfields.comment.field, 
                                inputfields.comment.value, 
                                inputfields.comment.formtitle, 
                                {
                                    'required':false,
                                    'add_field_html': error_helper,
                                    helper: inputfields.comment.formexplain,
                                }
                            ) }}
                        </div>

                         <!-- enabled -->
                         <div>
                            {% set error_helper %}
                                {% if inputfields.is_active.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.is_active.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.is_active.field,
                                inputfields.is_active.value,
                                inputfields.is_active.formtitle,
                               {
                                  'add_field_html': error_helper,
                                  helper: inputfields.is_active.formexplain,
                               }
                            ) }}
                        </div>

                        <!-- Debug mode -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.debug.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.debug.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.debug.field,
                                inputfields.debug.value,
                                inputfields.debug.formtitle,
                               {
                                  'add_field_html': error_helper,
                                  helper: inputfields.debug.formexplain,
                               }
                            ) }}
                        </div>
                    </div>
                </div>
            </div>
            <!-- ################################################################################-->
            <!-- transit tab -->
            <div class="tab-pane fade" id="transit" role="tabpanel" aria-labelledby="transit-tab">
                <div class="card-body">
                    <div class="row g-2">

                        <!-- compress_requests -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.compress_requests.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.compress_requests.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.compress_requests.field,
                                inputfields.compress_requests.value,
                                inputfields.compress_requests.formtitle,
                               {
                                  'add_field_html': error_helper,
                                   helper: inputfields.compress_requests.formexplain,
                               }
                            ) }}
                        </div>

                        <!-- compress_responses -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.compress_responses.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.compress_responses.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.compress_responses.field,
                                inputfields.compress_responses.value,
                                inputfields.compress_responses.formtitle,
                               {
                                  'add_field_html': error_helper,
                                   helper: inputfields.compress_responses.formexplain,
                               }
                            ) }}
                        </div>
                        
                        <!-- Proxied -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.proxied.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.proxied.errors }}</div>
                                {% endif %}
                                {% if inputfields.proxied.value == true %}
                                    {% if inputfields.strict.value == true %}
                                        <div class="form-text text-helper" style="color:rgb(180, 120, 8);">⚠️ Beware, misaligned proxy schemes might break the samlValidation in strict mode.<br>(<b>https</b>://external)=>(<b>http</b>://internal)</div>
                                    {% endif %}
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.proxied.field,
                                inputfields.proxied.value,
                                inputfields.proxied.formtitle,
                               {
                                  'add_field_html': error_helper,
                                   helper: inputfields.proxied.formexplain,
                               }
                            ) }}
                        </div>

                        <!-- validate_xml -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.validate_xml.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.validate_xml.errors }}</div>
                                {% endif %}
                                {% if inputfields.strict.value == false %}
                                    <div class="form-text text-helper" style="color:rgb(180, 120, 8);">⚠️ Strict is disabled, validate xml setting will not do anything.</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.validate_xml.field,
                                inputfields.validate_xml.value,
                                inputfields.validate_xml.formtitle,
                               {
                                  'add_field_html': error_helper,
                                   helper: inputfields.validate_xml.formexplain,
                               }
                            ) }}
                        </div>

                        <!-- validate_destination -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.validate_destination.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.validate_destination.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.validate_destination.field,
                                inputfields.validate_destination.value,
                                inputfields.validate_destination.formtitle,
                               {
                                  'add_field_html': error_helper,
                                   helper: inputfields.validate_destination.formexplain,
                               }
                            ) }}
                        </div>

                        <!-- lowercase_url_encoding -->
                        <div>
                            {% set error_helper %}
                                {% if inputfields.lowercase_url_encoding.errors == true %}
                                    <div class="form-text text-helper" style="color:red;">{{ inputfields.lowercase_url_encoding.errors }}</div>
                                {% endif %}
                            {% endset %}
                            {{ fields.sliderField(
                                inputfields.lowercase_url_encoding.field,
                                inputfields.lowercase_url_encoding.value,
                                inputfields.lowercase_url_encoding.formtitle,
                               {
                                  'add_field_html': error_helper,
                                   helper: inputfields.lowercase_url_encoding.formexplain,
                               }
                            ) }}
                        </div>

                    </div>
                </div>
            </div>

            <!-- ################################################################################-->
            <div class="tab-pane fade" id="provider" role="tabpanel" aria-labelledby="provider-tab">
                <div class="card-body">

                    <!-- Service provider details -->
                    <div>
                        <!-- Entity ID -->
                        {% set clipboad %}
                            <span class='btn btn-ghost-info' onclick="copyClip('EntityId')" onkeydown="copyClip('EntityId')">Copy Entity Id to clipboard</span>
                        {% endset %}
                        {{ fields.textField(
                            'EntityID', 
                            entityID, 
                            'Entity ID',
                            {
                                'id': 'EntityId',
                                'disabled':true,
                                'add_field_html': clipboad,
                                helper: __('You need to configure the Entity ID at the identity provider. This value is
                                            crosschecked by the identity provider to make sure the sign-in request is
                                            originating from the correct service provider.'),
                            }
                        ) }}

                        <!-- Meta URL -->
                        {% set clipboad %}
                            <span class='btn btn-ghost-info' onclick="copyClip('MetaUrl')" onkeydown="copyClip('MetaUrl')">Copy Meta Url to clipboard</span>
                            {% if inputfields.debug.value == false %}
                                <span class="form-help" data-bs-toggle="popover" data-bs-placement="top" data-bs-html="true" data-bs-content="{{ __("Debug is disabled, metadata will only be exposed if 'debug' is enabled.") }}">⚠️</span>
                            {% endif %}
                        {% endset %}
                        {{ fields.textField(
                            'MetaUrl', 
                            metaUrl, 
                            'MetaUrl',
                            {
                                'id': 'MetaUrl',
                                'disabled':true,
                                'add_field_html': clipboad,
                                helper: __('The location of the metadata provider for this service provider (GLPI). The meta location can be
                                            used by the identity provider to collect important information from this service provider like
                                            what public certificate to use for signing and encrypting the SamlResponse.'),
                            }
                        ) }}

                        <!-- ACS url -->
                        {% set clipboad %}
                            <span class='btn btn-ghost-info' onclick="copyClip('AcsUrl')" onkeydown="copyClip('AcsUrl')">Copy ACS Url to clipboard</span>
                        {% endset %}
                        {{ fields.textField(
                            'AcsUrl', 
                            acsUrl, 
                            'AcsUrl',
                            {
                                'id': 'AcsUrl',
                                'disabled':true,
                                'add_field_html': clipboad,
                                helper: __('The location of the Assertion Consumer Service (ACS) for this service provider (GLPI). 
                                            This url is called by the identity provider after succesfull login.'),
                            }
                        ) }}


                        <!-- Logout url -->
                        {% set clipboad %}
                            <span class='btn btn-ghost-info' onclick="copyClip('SloUrl')" onkeydown="copyClip('SloUrl')">Copy logout Url to clipboard</span>
                        {% endset %}
                        {{ fields.textField(
                            'SloUrl', 
                            sloUrl, 
                            'SloUrl',
                            {
                                'id': 'SloUrl',
                                'disabled':true,
                                'add_field_html': clipboad,
                                helper: __('The location where logout requests should be send back to from the Identity Provider.'),
                            }
                        ) }}
                    </div>                    <!-- sp_certificate -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.sp_private_key.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.sp_private_key.errors }}</div>
                            {% endif %}
                            {% if inputfields.sp_certificate.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.sp_certificate.errors }}</div>
                            {% endif %}
                            {# make sure validate is an array/object before accessing nested keys #}
                            {% set sp_validate = inputfields.sp_certificate.validate|default({}) %}
                            {% if sp_validate.subject.CN|default(false) == true %}
                                <div class="form-text text-helper" style="color:rgb(71, 180, 8);">🆗 Valid certificate found: {{ sp_validate.subject.CN|default('') }}</div>
                            {% endif %}
                            {% if sp_validate.validations.validTo|default(false) == true %}
                                <div class="form-text text-helper" style="color:rgb(180, 120, 8);">{{ sp_validate.validations.validTo|default('') }}</div>
                            {% endif %}
                            {% if sp_validate.validations.validFrom|default(false) == true %}
                                <div class="form-text text-helper" style="color:rgb(180, 120, 8);">{{ sp_validate.validations.validFrom|default('') }}</div>
                            {% endif %}
                        {% endset %}
                        
                        {{ fields.textareaField(
                            inputfields.sp_certificate.field,
                            inputfields.sp_certificate.value,
                            inputfields.sp_certificate.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.sp_certificate.formexplain,
                            }) 
                        }}
                    </div>

                    <!-- sp_private_key -->
                    <div>
                        {{ fields.textareaField( 
                            inputfields.sp_private_key.field,
                            inputfields.sp_private_key.value,
                            inputfields.sp_private_key.formtitle,
                            {
                                helper: inputfields.sp_private_key.formexplain,
                            }) 
                        }}
                    </div>
                    
                    <div>
                        {% set error_helper %}
                            {% if inputfields.sp_nameid_format.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.sp_nameid_format.errors }}</div>
                            {% endif %}
                        {% endset %}

                        {{ fields.dropdownArrayField(
                            inputfields.sp_nameid_format.field,
                            inputfields.sp_nameid_format.value,
                            inputOptionsNameFormat,
                            inputfields.sp_nameid_format.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.sp_nameid_format.formexplain,
                            }
                        ) }}
                    </div>
                </div>
            </div>

            <!-- ################################################################################-->
            <!-- Identity provider details -->
            <div class="tab-pane fade" id="idp" role="tabpanel" aria-labelledby="idp-tab">
                <div class="card-body">
                     <!-- idp_entity_id -->
                     <div>
                        {% set error_helper %}
                            {% if inputfields.idp_entity_id.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.idp_entity_id.errors }}</div>
                            {% endif %}
                        {% endset %}

                        {{ fields.textField( 
                            inputfields.idp_entity_id.field, 
                            inputfields.idp_entity_id.value, 
                            inputfields.idp_entity_id.formtitle, 
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.idp_entity_id.formexplain,
                            }) 
                        }}
                    </div>

                    <!-- idp_single_sign_on_service -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.idp_single_sign_on_service.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.idp_single_sign_on_service.errors }}</div>
                            {% endif %}
                        {% endset %}

                        {{ fields.textField( 
                            inputfields.idp_single_sign_on_service.field, 
                            inputfields.idp_single_sign_on_service.value, 
                            inputfields.idp_single_sign_on_service.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.idp_single_sign_on_service.formexplain,
                            }) 
                        }}
                    </div>

                    <!-- idp_single_logout_service -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.idp_single_logout_service.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.idp_single_logout_service.errors }}</div>
                            {% endif %}
                        {% endset %}

                        {{ fields.textField( 
                            inputfields.idp_single_logout_service.field, 
                            inputfields.idp_single_logout_service.value, 
                            inputfields.idp_single_logout_service.formtitle, 
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.idp_single_logout_service.formexplain,
                            }) 
                        }}
                    </div>

                    <!-- idp_certificate -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.idp_certificate.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.idp_certificate.errors }}</div>
                            {% endif %}
                            {% if inputfields.idp_certificate.validate.subject.CN == true %}
                                <div class="form-text text-helper" style="color:rgb(71, 180, 8);">🆗 Valid Certificate found: {{ inputfields.idp_certificate.validate.subject.CN }}</div>
                            {% endif %}
                            {% if inputfields.idp_certificate.validate.validations.validTo == true %}
                                <div class="form-text text-helper" style="color:rgb(180, 140, 8);">{{ inputfields.idp_certificate.validate.validations.validTo }}</div>
                            {% endif %}
                            {% if inputfields.idp_certificate.validate.validations.validFrom == true %}
                                <div class="form-text text-helper" style="color:rgb(180, 140, 8);">{{ inputfields.idp_certificate.validate.validations.validFrom }}</div>
                            {% endif %}
                        {% endset %}

                        {{ fields.textareaField( 
                            inputfields.idp_certificate.field, 
                            inputfields.idp_certificate.value, 
                            inputfields.idp_certificate.formtitle, 
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.idp_certificate.formexplain,
                            }) 
                        }}
                    </div>

                    <!-- requested_authn_context -->
                    <div>
                        <div class="form-field row col-12 col-sm-6  mb-2">
                            <div class="col-form-label col-xxl-5 text-xxl-end">
                                <label for="{{ inputfields.requested_authn_context.field }}">{{ inputfields.requested_authn_context.formtitle }}</label>
                                <span class="form-help" data-bs-toggle="popover" data-bs-placement="top" data-bs-html="true"
                            data-bs-content="{{ inputfields.requested_authn_context.formexplain }}">?</span>
                            </div>
                            <div class="col-xxl-7  field-container">
                                <!--
                                @see: https://codeberg.org/QuinQuies/glpisaml/issues/14
                                Todo: add nicer, userfriendly drag-drop element
                                -->
                                <select id="keep-order" multiple="multiple" aria-label="multiple select" name="{{ inputfields.requested_authn_context.field }}[]">
                                    {% for key, authn in inputOptionsAuthnContext %}
                                            {%set selected = false %}
                                            {% for value in inputfields.requested_authn_context.value %}
                                                {% if value == key %}
                                                    {% set selected = 'selected' %}
                                                {% endif %}
                                            {% endfor %}
                                                <option {{ selected }} value="{{ key }}">{{ authn }}</option>
                                        {% endfor %}
                                </select>
                            </div>
                        </div>
                    </div>

                    <!-- requested_authn_context -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.requested_authn_context_comparison.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.requested_authn_context_comparison.errors }}</div>
                            {% endif %}
                        {% endset %}

                        {{ fields.dropdownArrayField( 
                            inputfields.requested_authn_context_comparison.field,
                            inputfields.requested_authn_context_comparison.value,
                            inputOptionsAuthnCompare,
                            inputfields.requested_authn_context_comparison.formtitle, 
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.requested_authn_context_comparison.formexplain,
                            }) 
                        }}
                       
                    </div>

                </div>
            </div>

            <!-- ################################################################################-->
            <!-- Security details -->
            <div class="tab-pane fade" id="security" role="tabpanel" aria-labelledby="security-tab">
                <div class="card-body">
                    <!-- enforce SSO -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.enforce_sso.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.enforce_sso.errors }}</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.enforce_sso.field,
                            inputfields.enforce_sso.value,
                            inputfields.enforce_sso.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.enforce_sso.formexplain,
                            }
                        ) }}
                    </div>

                    <!-- Strict mode -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.strict.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.strict.errors }}</div>
                            {% endif %}
                            {% if inputfields.strict.value == false %}
                                        <div class="form-text text-helper" style="color:rgb(180, 120, 8);">⚠️ Your environment is at risk, make sure strict is enabled!</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.strict.field,
                            inputfields.strict.value,
                            inputfields.strict.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.strict.formexplain,
                            }
                        ) }}
                    </div>
                    
                    <!-- User JIT -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.user_jit.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.user_jit.errors }}</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.user_jit.field,
                            inputfields.user_jit.value,
                            inputfields.user_jit.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.user_jit.formexplain,
                            }
                        ) }}
                    </div>
                    
                    <!-- security_nameidencrypted -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.security_nameidencrypted.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.security_nameidencrypted.errors }}</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.security_nameidencrypted.field,
                            inputfields.security_nameidencrypted.value,
                            inputfields.security_nameidencrypted.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.security_nameidencrypted.formexplain,
                            }) 
                        }}
                    </div>

                    

                    <!-- security_authnrequestssigned -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.security_authnrequestssigned.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.security_authnrequestssigned.errors }}</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.security_authnrequestssigned.field,
                            inputfields.security_authnrequestssigned.value,
                            inputfields.security_authnrequestssigned.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.security_authnrequestssigned.formexplain,
                            }) 
                        }}
                    </div>

                        

                    <!-- security_logoutrequestsigned -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.security_logoutrequestsigned.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.security_logoutrequestsigned.errors }}</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.security_logoutrequestsigned.field,
                            inputfields.security_logoutrequestsigned.value,
                            inputfields.security_logoutrequestsigned.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.security_logoutrequestsigned.formexplain,
                            }) 
                        }}
                    </div>
    
                    

                    <!-- security_logoutresponsesigned -->
                    <div>
                        {% set error_helper %}
                            {% if inputfields.security_logoutresponsesigned.errors == true %}
                                <div class="form-text text-helper" style="color:red;">{{ inputfields.security_logoutresponsesigned.errors }}</div>
                            {% endif %}
                        {% endset %}
                        {{ fields.sliderField(
                            inputfields.security_logoutresponsesigned.field,
                            inputfields.security_logoutresponsesigned.value,
                            inputfields.security_logoutresponsesigned.formtitle,
                            {
                                'add_field_html': error_helper,
                                helper: inputfields.security_logoutresponsesigned.formexplain,
                            }) 
                        }}
                    </div>
                </div>
            </div>
            <!-- ################################################################################-->
            <!-- SCIM details -->
            <div class="tab-pane fade" id="scim" role="tabpanel" aria-labelledby="scim-tab">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-7">
                            <div class="row g-2">
                                <!-- scim_active -->
                                <div>
                                    {% set error_helper %}
                                        {% if inputfields.scim_active.errors == true %}
                                            <div class="form-text text-helper" style="color:red;">{{ inputfields.scim_active.errors }}</div>
                                        {% endif %}
                                    {% endset %}
                                    {{ fields.sliderField(
                                        inputfields.scim_active.field,
                                        inputfields.scim_active.value,
                                        inputfields.scim_active.formtitle,
                                        {
                                            'add_field_html': error_helper,
                                            helper: inputfields.scim_active.formexplain,
                                        }
                                    ) }}
                                </div>

                                <!-- scim_token -->
                                <div>
                                    {% set error_helper %}
                                        {% if inputfields.scim_token.errors == true %}
                                            <div class="form-text text-helper" style="color:red;">{{ inputfields.scim_token.errors }}</div>
                                        {% endif %}
                                    {% endset %}
                                    {{ fields.textField(
                                        inputfields.scim_token.field,
                                        inputfields.scim_token.value,
                                        inputfields.scim_token.formtitle,
                                        {
                                            'add_field_html': error_helper,
                                            helper: inputfields.scim_token.formexplain,
                                        }
                                    ) }}
                                </div>

                                <!-- SCIM URL -->
                                <div>
                                    {% set clipboad %}
                                        <span class='btn btn-ghost-info' onclick="copyClip('ScimUrl')" onkeydown="copyClip('ScimUrl')">Copy SCIM Url to clipboard</span>
                                    {% endset %}
                                    {{ fields.textField(
                                        'ScimUrl', 
                                        scimUrl, 
                                        'SCIM Endpoint URL',
                                        {
                                            'id': 'ScimUrl',
                                            'disabled':true,
                                            'add_field_html': clipboad,
                                            helper: __('This is the SCIM endpoint URL you need to provide to your Identity Provider to enable user and group provisioning.'),
                                        }
                                    ) }}
                                </div>
                            </div>
                        </div>

                        <!-- Attribute Mapping Reference -->
                        <div class="col-md-5">
                            <div class="alert alert-info shadow-sm">
                                <h5><i class="fas fa-info-circle"></i> {{ __('Attribute Mapping Reference', plugin) }}</h5>
                                <p class="small text-muted">{{ __('Use these GLPI field names and corresponding SAML/SCIM attributes when configuring your IdP:', plugin) }}</p>
                                <div class="table-responsive">
                                    <table class="table table-sm table-striped small align-middle mb-0">
                                        <thead>
                                            <tr>
                                                <th>{{ __('GLPI Field', plugin) }}</th>
                                                <th>{{ __('SAML Claim (Short)', plugin) }}</th>
                                                <th>{{ __('SCIM Attribute', plugin) }}</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <tr><td><code>name</code></td><td><code>NameID</code></td><td><code>userName</code></td></tr>
                                            {# <tr><td><code>authid</code></td><td><code>.../objectidentifier</code></td><td><code>externalId</code></td></tr> #}
                                            <tr><td><code>_useremails</code></td><td><code>.../emailaddress</code></td><td><code>emails</code></td></tr>
                                            <tr><td><code>realname</code></td><td><code>.../surname</code></td><td><code>name.familyName</code></td></tr>
                                            <tr><td><code>firstname</code></td><td><code>.../givenname</code></td><td><code>name.givenName</code></td></tr>
                                            <tr><td><code>phone</code></td><td><code>.../telephonenumber</code></td><td><code>phoneNumbers[type eq "work"]</code></td></tr>
                                            <tr><td><code>mobile</code></td><td><code>.../mobilephone</code></td><td><code>phoneNumbers[type eq "mobile"]</code></td></tr>
                                            <tr><td><code>jobtitle</code></td><td><code>.../jobtitle</code></td><td><code>title</code></td></tr>
                                            <tr><td><code>is_active</code></td><td>-</td><td><code>active</code></td></tr>
                                            <tr><td><code>groups</code></td><td><code>.../claims/groups</code></td><td><code>groups</code></td></tr>
                                        </tbody>
                                    </table>
                                </div>
                                {# <p class="x-small text-muted mt-3 mb-0">
                                    <i class="fas fa-lightbulb"></i> {{ __('Tip: Mapping "externalId" (SCIM) or "objectidentifier" (SAML) to "authid" ensures users remain linked even if their username or email changes.', plugin) }}
                                </p> #}
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- ################################################################################-->
            <!-- Logging details -->
            <div class="tab-pane fade" id="logging" role="tabpanel" aria-labelledby="logging-tab">
                <div class="card-body">
                    <h2>{{ __("Work in progress, logging is by no means complete but still usable. All feedback more than welcome!") }}</h2>
                    <div class="mb-2">{{ __("Phases: 1 = initial, 2=send_saml_SSO_request, 3=performed_saml_auth, 4=performed_glpi_auth, 5=file_exclude, 6=forced_logoff, 7=timed_out, 8=manual_glpi_logout") }}
                    </div>
                    <div class="table-responsive">
                        <table class="table table-striped table-sm">
                            <thead>
                                <tr>
                                <th>Id</th>
                                <th>IdpId</th>
                                <th>username</th>
                                <th>glpiAuthed</th>
                                <th>samlAuthed</th>
                                <th>Phase</th>
                                <th>loginTime</th>
                                <th>lastClickTime</th>
                                <th>Locations</th>
                                <th>LoginFlowTrace</th>
                                </tr>
                            </thead>
                            {% for logrow in loggingfields %}
                            <tr>
                                <td>{{ logrow.id }}</td>
                                <td>{{ logrow.idpId }}</td>
                                <td>{{ logrow.userName }}</td>
                                <td>{{ logrow.glpiAuthed }}</td>
                                <td>{{ logrow.samlAuthed }}</td>
                                <td>{{ logrow.phase }}</td>
                                <td>{{ logrow.loginTime }}</td>
                                <td>{{ logrow.lastClickTime }}</td>
                                <td> <span class="form-help" data-bs-toggle="popover" data-bs-placement="top" data-bs-html="true"
                                    data-bs-content="<pre>{{ logrow.location }}</pre>">?</span></td>
                                <td>
                                <span class="form-help" data-bs-toggle="popover" data-bs-placement="top" data-bs-html="true"
                                    data-bs-content="<pre>{{ logrow.loginFlowTrace }}</pre>">?</span>
                                </td>
                            </tr>
                            {% endfor %}
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="card itil-footer" style="margin-top:10px;">
        <div class="form-buttons col-lg d-flex ms-auto ms-lg-0 flex-row-reverse p-1 ">
            <div class="m-1">
                <button class="btn btn-primary " type="submit" name="update" title="Save">
                    <i class="far fa-save"></i>
                    <span class="d-none d-xl-block">{{ __("Save") }}</span>
                    </button>
            </div>
            <div class="m-1">
                <button class="btn btn-ghost-danger" type="submit" name="delete" title="" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-original-title="Put in trashbin">
                    <i class="ti ti-trash"></i>
                    <span class="d-none d-xl-block">{{ __("Delete") }}</span>
                    </button>
            </div>
            {% if inputfields.debug.value == true %}
            <div class="m-1 align-middle text-muted pull-left pt-2">
                ⚠️ Warning SP metadata exposed! - ROWID: {{ inputfields.id.value }} | CREATED DATE: {{ inputfields.date_creation.value }} | LAST UPDATED: {{ inputfields.date_mod.value }}
            </div>
            {% endif %}
        </div>
    </div>
</div>
{{ close_form|raw }}